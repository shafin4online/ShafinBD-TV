{
  "entities": {
    "channel": {
      "title": "Channel",
      "description": "An IPTV stream channel stored in the cloud-synced database",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier of the channel"
        },
        "name": {
          "type": "string",
          "description": "Display name of the channel"
        },
        "url": {
          "type": "string",
          "description": "M3U8 stream URL or MP4 source URL"
        },
        "category": {
          "type": "string",
          "description": "Category group of the channel for categorization"
        },
        "logoUrl": {
          "type": "string",
          "description": "Optional HTTPS image URL to represent the station channel logo"
        },
        "description": {
          "type": "string",
          "description": "Optional text description of the channel"
        },
        "groupTitle": {
          "type": "string",
          "description": "Optional parent network title group"
        },
        "order": {
          "type": "integer",
          "description": "Order rank weight to structure channels position within lists"
        },
        "createdAt": {
          "type": "string",
          "description": "Creation timestamp identifier"
        }
      },
      "required": ["id", "name", "url", "category"]
    },
    "popupConfig": {
      "title": "PopupConfig",
      "description": "Controlling settings structure for showing custom timer-based forced overlay/popup redirections",
      "type": "object",
      "properties": {
        "fbPopupMinutes": { "type": "integer" },
        "fbPopupText": { "type": "string" },
        "facebookLink": { "type": "string" },
        "customPopupMinutes": { "type": "integer" },
        "customPopupText": { "type": "string" },
        "customPopupLink": { "type": "string" },
        "controlSystemEnabled": { "type": "boolean" }
      },
      "required": [
        "fbPopupMinutes",
        "fbPopupText",
        "facebookLink",
        "customPopupMinutes",
        "customPopupText",
        "customPopupLink",
        "controlSystemEnabled"
      ]
    },
    "categoriesConfig": {
      "title": "CategoriesConfig",
      "description": "Holding global list of categories and their custom ordering of appearance",
      "type": "object",
      "properties": {
        "order": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["order"]
    }
  },
  "firestore": {
    "channels": {
      "schema": "channel",
      "description": "Collection of custom IPTV channels synced across all players"
    },
    "configs": {
      "schema": "popupConfig",
      "description": "Document collection holding global application configuration states"
    }
  }
}
